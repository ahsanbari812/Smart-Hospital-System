services:
  - type: web
    name: smart-hospital-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://smart-hospital-backend-ul9j.onrender.com/

